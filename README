To play this game you need the following eggs:

- doodle (developpement version, available at
  https://bitbucket.org/ckeen/doodle.git)

- matchable

- cairo (with a patch that add a few missing functions: just
  `chicken-install -r cairo`, apply cairo.patch and run chicken-install in the
  cairo directory.
